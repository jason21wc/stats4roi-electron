# The DMG maker depends on a native module compiled for one Node major
# version (see scripts/check-build-node.js). Refuse to install under the
# wrong one, which would compile it for the wrong version silently.
engine-strict=true
